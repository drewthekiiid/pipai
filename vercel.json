{
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --prefix . && npm install --prefix apps/web",
  "buildCommand": "npx prisma generate --schema=prisma/schema.prisma && cd apps/web && npm run build",
  "packageManager": "npm"
}
