{
  "name": "@pipai/api",
  "version": "1.0.0",
  "description": "PIP AI Edge Upload API",
  "main": "main.py",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step needed for Python'",
    "test": "pytest tests/ -v"
  }
}
